<?xml version="1.0" encoding="UTF-8" ?>
<FlyoutPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local1="clr-namespace:Soccer_App.View"
    x:Class="Soccer_App.View.MyFlyoutPage"
    Title="Menu"
    BackgroundColor="#FDCB04">
    <FlyoutPage.Flyout>

       <ContentPage Title="Options" Icon="hamburger.png">

           <ListView x:Name="menu"/>
            <!--<ListView x:Name="menu"
                      Background="#FDCB04" ItemsSource="{Binding MyFlyOutMenu}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout>
                            <Image Source="{Binding IconSource}"/>
                            <Label Text="{Binding Title}" />

                        </StackLayout>

                    </DataTemplate>

                </ListView.ItemTemplate>

            </ListView>-->
        </ContentPage>

    </FlyoutPage.Flyout>

    <FlyoutPage.Detail>

        <ContentPage>
            <Label Text="Detail"/>
        </ContentPage>
    </FlyoutPage.Detail>
    
</FlyoutPage>

